spring:
  datasource:
    url: jdbc:h2:mem:testdb
#    initialization-mode: never
  sql:
    init:
      mode: always


  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop
    show-sql: false

logging:
  level:
    ROOT: ERROR

